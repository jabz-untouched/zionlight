/**
 * Store Barrel Export
 * Zustand stores for global state management
 * Philosophy: Minimal global state, prefer server state (React Query) and local state
 */

export { useUIStore } from "./ui.store";
